{
  "description": "Blocks direct file edits in main context - enforces delegation to agents",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": {
          "tool_name": "Edit"
        },
        "hooks": [
          {
            "type": "command",
            "command": "echo '🚫 ORCHESTRATOR MODE: Direct edits are blocked. Use Task tool with an agent instead.' && exit 1",
            "timeout": 5
          }
        ]
      },
      {
        "matcher": {
          "tool_name": "Write"
        },
        "hooks": [
          {
            "type": "command",
            "command": "echo '🚫 ORCHESTRATOR MODE: Direct writes are blocked. Use Task tool with an agent instead.' && exit 1",
            "timeout": 5
          }
        ]
      },
      {
        "matcher": {
          "tool_name": "MultiEdit"
        },
        "hooks": [
          {
            "type": "command",
            "command": "echo '🚫 ORCHESTRATOR MODE: Direct multi-edits are blocked. Use Task tool with an agent instead.' && exit 1",
            "timeout": 5
          }
        ]
      },
      {
        "matcher": {
          "tool_name": "NotebookEdit"
        },
        "hooks": [
          {
            "type": "command",
            "command": "echo '🚫 ORCHESTRATOR MODE: Direct notebook edits are blocked. Use Task tool with an agent instead.' && exit 1",
            "timeout": 5
          }
        ]
      }
    ]
  }
}
